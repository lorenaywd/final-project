{# <form method="post" action="{{ path('app_type_operation_delete', {'id': type_operation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_operation.id) }}">
    <button style="float: right;" class="btn supprimer m-3">Delete</button>
</form> #}
